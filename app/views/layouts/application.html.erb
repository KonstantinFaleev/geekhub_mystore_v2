<!DOCTYPE html>
<html>
  <head>
    <title><%= if content_for?(:title) then yield(:title) + ' | ' end %>Geekhub Mystore</title>
    <%= csrf_meta_tags %>
    <%= display_meta_tags %>
    <meta name='description'
          content='<%= yield_meta_tag(:description, 'Default description') %>' />
    <meta name='keywords'
          content='<%= yield_meta_tag(:keywords, 'defaults,ruby,rails') %>' />
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class='<%= controller.controller_name %>'>
  <div id="banner">
    <%= @page_title || "GeekHub My Store" %>
    <%#= image_tag 'logo.svg', alt: 'Geekhub Mystore' %>
    <span class="title"><%= @page_title %></span>
  </div>
  <div id="columns">
    <div id="side">
      <div id="cart">
        <% if @cart %>
            <%= hidden_div_if(@cart.line_items.empty?, id: 'cart') do %>
              <%= render @cart %>
            <% end %>
        <% end %>
      </div>
      <ul>
        <li><%= link_to 'Home', store_index_path %></li>
        <li><a href="https://ru.stackoverflow.com/questions/tagged/ruby-on-rails">Questions</a></li>
        <li><a href="http://rubyonrails.org/">News</a></li>
        <li><a href="http://geekhub.ck.ua/contacts/">Contact</a></li>
      </ul>
    </div>
      <div id="main">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
